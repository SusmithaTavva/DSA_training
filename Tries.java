we have to create a node which can store a character array of 26 characters
the last node need to change the flag to true
  ADV:
to get faster retrivals of the strings
  class node{
      int[26] data;
      boolean flag;
      Node next;
